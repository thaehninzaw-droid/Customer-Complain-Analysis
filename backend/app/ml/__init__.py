"""
Trained-model layer for both algorithms in the flowchart:
  Algorithm 1 - category classification  (train_classifier.py / classifier_model.py)
  Algorithm 2 - priority prediction      (train_priority.py / priority_model.py)

Everything in here is OPTIONAL at runtime: app/classify.py and
app/priority.py both work fine with zero files in artifacts/ (they
fall back to their rule-based baselines). Run the two train_*.py
scripts once to produce real trained models; see docs/ALGORITHMS.md.
"""
